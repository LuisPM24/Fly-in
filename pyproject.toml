[project]
name = "fly-in"
version = "1.0.0"
requires-python = ">=3.10"
description = ""
authors = [
    {name = "lupalomi"}
]
dependencies = [
    "mypy",
    "flake8",
    "pytest",
    "pydantic"
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
